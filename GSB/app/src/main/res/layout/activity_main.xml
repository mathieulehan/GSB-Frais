<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="sio.gsbfrais.MainActivity"
    android:background="@android:color/white">

    <Button
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="Rechercher par départements"
        android:id="@+id/searchByDep"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:text="Rechercher par nom"
        android:id="@+id/searchByName"
        android:layout_below="@+id/searchByDep"
        android:layout_alignLeft="@+id/searchByDep"
        android:layout_alignStart="@+id/searchByDep" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageView"
        android:background="@drawable/logo"
        android:layout_above="@+id/searchByDep"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
